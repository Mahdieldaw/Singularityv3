
/**
 * DEPRECATED: This sync mechanism is no longer needed as the cognitive pipeline
 * feature flag has been removed in favor of `request.mode` routing.
 * keeping this file empty to preserve file structure until full cleanup.
 */
export function useExperimentalFlagSync() {
    // No-op
}
